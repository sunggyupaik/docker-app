sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t gyu/docker-react-app -f Dockerfile.dev .

script:
  - docker run -e CI=tue gyu/docker-react-app npm run test -- --coverage

after_success:
  - echo "Test success"
